import { cva } from 'class-variance-authority'

export const homeContainer = cva(`
  font-text font-medium antialiased text-base text-gray-800 min-h-screen
`)

export const header = cva(`
  w-full bg-magenta-500 h-20
`)
